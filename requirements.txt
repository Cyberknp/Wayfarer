# This project is a static frontend site relying on external CDNs for React and Tailwind.
# Currently, there are no backend Python dependencies required to run the project.

# Software utilized for local execution/testing:
# - Python 3.x (for the built-in HTTP server module)
# Local development server command:
# python -m http.server 8000
